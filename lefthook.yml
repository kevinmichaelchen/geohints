pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,jsx,ts,tsx}"
      run: npx oxlint {staged_files}
    format:
      glob: "*.{js,jsx,ts,tsx,json,md,yaml,yml,css}"
      run: npx oxfmt {staged_files}
      stage_fixed: true
